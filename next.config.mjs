/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: true,
  },
  images: {
    domains: ["xsvrfargkdsggggsbsfa.supabase.co"],
  },
};

export default nextConfig;
